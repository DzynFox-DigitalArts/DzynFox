const Portfolio = ({name}) => {
    return(
        <section className="Portfolio">
            <h1 className="section-header">Have a look at our {name} portfolio.</h1>
        </section>
    )
}

export default Portfolio